# 执行日志

## 用户原始 prompt

浏览器评论：Add 页 Serving 区域中，变化的应该是上面大的卡路里；这里应该显示一碗默认是多少卡路里，相当于是个静态值。

## 启动运行时的分支和版本

- 分支：`main`
- 版本：`92c62344b37cdad8f965e15cc1f5f763fb306e7b`
- 任务开始时间：2026-06-18 17:25:00 +0800
- 任务结束时间：2026-06-18 17:27:50 +0800
- 任务结束时是否执行了提交：否

## 已阅读上下文

- `AGENTS.md`
- `REQUIREMENTS.md`
- `reference/ts/src/main.tsx`
- `reference/ts/src/styles.css`

## 对话与行动记录

- 检查 Add 页 `EditableDetectedFood` 的 Serving 编辑区。
- 将左侧说明从动态份量改为静态基准热量，例如 `1 bowl / 360 kcal`。
- 保持右侧 stepper 中间输入动态份量，例如 `0.8 bowl`。
- 保持上方主食物卡根据份量实时显示变化后的 kcal。
- 更新 `REQUIREMENTS.md`。

## 完成工作

- `reference/ts/src/main.tsx`
  - 给 `EditableDetectedFood` 增加 `kcalPerServing` 参数。
  - Serving 左侧显示每单位默认热量。
  - 上方主卡 kcal 继续使用当前份量计算值。
- `reference/ts/src/styles.css`
  - 微调 Serving 左侧静态说明宽度和不换行。
- `REQUIREMENTS.md`
  - 标记 Serving 左侧显示每份默认热量静态基准值。

## 更新的需求 ID

- `WEB-UX-D-002`

## 更新的 README 或 DESIGN 章节

- 无。

## 验证方式

- 在 `reference/ts/` 执行 `npm run build`，构建通过。
- 访问 `http://127.0.0.1:5173/?v=202606181728#add` 返回 `200 OK`。

## 备注

- 本次仅调整 Serving 区域的信息层级，不改变保存逻辑。
